# SynClip Desktop Configuration

# The port where the local server should run if you run the backend on this machine
PORT=5001

# The fixed room code to request from the backend server.
# Set this to any custom code (e.g. ABHAY1). If not set, it defaults to ABHAY1.
ROOM_ID=ABHAY1
